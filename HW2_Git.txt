GitHub. HW_2
1. Ќа локальном репозитории сделать ветки дл€:
- Postman
- Jmeter
- CheckLists
- Bag Reports
- SQL
- Charles
- Mobile testing - git branch

2. «апушить все ветки на внешний репозиторий - git push --all
3. ¬ ветке Bag Reports сделать текстовый документ со структурой баг репорта - cat >> BagReport.txt
4. «апушить структуру багрепорта на внешний репозиторий - git add . git commit 
5. ¬мержить ветку Bag Reports в Main - git merge BagReports -m "merge bagrep"
6. «апушить main на внешний репозиторий. - git commit -am git push
7. ¬ ветке CheckLists набросать структуру чек листа. - cat >> 
8. «апушить структуру на внешний репозиторий - git add . git commit
9. Ќа внешнем репозитории сделать Pull Request ветки CheckLists в main
10. —инхронизировать ¬нешнюю и Ћокальную ветки Main - git pull

нова€ ветка git branch
нова€ ветка с переходом git checkout -b
переход на ветку git checkout
замержить (копировать) ветку в другую ветку (наход€сь в ветке в которую мержим) git merge название ветки которую мержим -m "messege" 
запушить ветку git push -u origin название
git checkout CheckLists
cat > CheckList.txt
git add .
git commit -m "add checklist"
git merge CheckLists 
git push
git push -u origin CheckLists

создать репозиторий локально - git init 
git remote add origin https://github.com/IlyaPluh/Local.git
git branch -M master
git push -u origin master

